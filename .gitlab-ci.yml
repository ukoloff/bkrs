image: node:5

cache:
  paths:
  - node_modules/

test:
  before_script:
  - apt-get update -qq
  - apt-get install -y dictzip zip
  - node -v
  - npm -v
  - npm install

  script:
  - npm test
  - node lib/fetch
  - npm start --tags=dsl
  - npm start
